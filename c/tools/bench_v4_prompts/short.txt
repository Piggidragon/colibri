You are reviewing a small inference server for a single workstation. Explain how a memory planner should divide 32 GiB of system RAM among operating-system headroom, model runtime buffers, dense weights, an output head, and a streamed expert cache. The machine has an Intel desktop CPU, dual-channel DDR4 memory, one fast NVMe drive, one slower SSD, and a headless 12 GiB NVIDIA GPU. The model uses a mixture-of-experts architecture, so cache misses require large expert records to be read from storage while cache hits remain in RAM.

Give a concise engineering note that distinguishes a planner budget from an operating-system-enforced memory limit. Discuss why setting the process budget above MemAvailable can silently restore the automatic reserve path, why a scratch allowance is only accounting headroom, and why context length must match the workload. Include a short validation checklist covering deterministic generation, cold and warm cache runs, expert hit rate, bytes read per token, time to first token, decode throughput, and token-identical regression tests. Do not propose changing router choices or model precision merely to improve a benchmark.

End with one paragraph describing how you would recognize an impossible throughput result from the memory-bandwidth ceiling and how you would investigate it before publishing a number.

Use concrete units throughout. Distinguish decimal gigabytes used for storage traffic from binary gibibytes used for capacity planning. Note that expert-cache growth is quantized in whole slots across all sparse layers, so a reserve reduction does not necessarily appear as an equal byte-for-byte increase in the final cache. Mention that a headless GPU budget must still leave room for driver state and workspaces, even though this particular baseline keeps V4 tensors in host memory.

Finally, state what belongs in the benchmark artifact: source revision and dirty state, prompt profile and hash, actual prompt and generated token counts, context bound, cache state, thread count, all explicit V4 environment knobs, planner tiers, KV capacity, speculative-drafting status, and raw expert counters. Explain why an unavailable checkpoint or missing test dependency must be reported as pending rather than replaced with an estimate.
